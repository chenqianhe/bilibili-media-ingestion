# Local pytest defaults for the dedicated test Postgres container.
# backend/tests/conftest.py loads this automatically for host-based pytest runs.
POSTGRES_SERVER=localhost
POSTGRES_PORT=55432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis
